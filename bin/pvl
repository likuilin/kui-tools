#!/usr/bin/env nix-shell
#!nix-shell --pure -i fish -p fish pv

function intr --on-signal SIGINT
  echo "sigint - push again to exit"
  sleep 1 || exit
end

while set pid (pidof -s "$argv[1]")
  echo Watching "$argv[1]" "$pid" "out of" (pidof "$argv[1]")
  pv -d "$pid"
  echo ""
end
